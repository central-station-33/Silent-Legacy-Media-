{
  "title": "Silent Legacy News Scout input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "feedUrls": {
      "title": "RSS/Atom feed URLs",
      "type": "array",
      "editor": "stringList",
      "description": "Local news outlets, court-record feeds, and press-release wires to poll.",
      "prefill": [
        "https://www.prnewswire.com/rss/all-news-releases-list.rss",
        "https://www.businesswire.com/portal/site/home/news/rss/"
      ]
    },
    "makeWebhookUrl": {
      "title": "Make.com webhook URL",
      "type": "string",
      "editor": "textfield",
      "description": "Destination for new-item payloads. Falls back to the MAKE_WEBHOOK_URL env var."
    },
    "lookbackHours": {
      "title": "Lookback window (hours)",
      "type": "integer",
      "default": 3,
      "description": "Only push items published within this many hours of the run."
    }
  },
  "required": ["feedUrls"]
}
